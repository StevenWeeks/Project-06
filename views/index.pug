extends layout.pug

block content
  article.grid-container.portfolio-intro
    div

      Title Fullstack Javascript Developer


      p.lead.text-light
        | This portfolio is made with express for creating routes and rendering pug templates.  This shows the projects I've done so far and a spot for future projects I'll be working on to show my progress as a developer.
  article.grid-container.portfolio-index

   .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3

    each project in data.project
      .cell
       a(href=`/project${project.id}`)
        h5  #{project.project_name}
          img.thumbnail(src=`${project.image_urls[0]}`)
